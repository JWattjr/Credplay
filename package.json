{
  "name": "credplay-monorepo",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter credplay dev",
    "dev:backend": "pnpm --filter credplay-backend dev",
    "build:frontend": "pnpm --filter credplay build",
    "build:backend": "pnpm --filter credplay-backend build",
    "install:all": "pnpm install"
  }
}
